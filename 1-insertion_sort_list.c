#include "sort.h"

/**
  * insertion_sort_list - sorts a list based on insertion sorting
  * 
  * @list: double linked list to be sorted
  * Return: void, no returns
  */

void insertion_sort_list(listint_t **list)
{
	listint_t temp;
	listint_t check;

	if (list == NULL)
		break;
	temp = malloc(sizeof(listint_t));
	if (temp == NULL)
		break;
	check = malloc(sizeof(listint_t));
	if  (to_change == NULL)
		break;

	temp = list;
	check = list;

	while (temp->next != NULL)
	{
		if (temp->prev 
	}

}
